import random

#generates the random whole number between the range 1 to 15
print(random.randint(1, 15))

#generates the real number between 0 to 1
print(random.random())

#generates the real number between the 1 to 100
print(random.uniform(1,100))